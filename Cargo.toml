[package]
name = "llm-rs"
version = "0.1.0"
edition = "2024"
authors = ["phildav"]

[dependencies]
glob = "0.3.2"
rand_mt = "5.0.0"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
overflow-checks = false

[lib]
path = "src/lib.rs"
